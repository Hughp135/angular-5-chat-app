version: "2"
services:
  app:
    container_name: chatapp
    restart: "no"
    build: .
    ports:
      - "7205:7202"
    links:
      - mongo
    environment:
      - NODE_ENV=production
  mongo:
    container_name: mongo
    image: mongo
    ports:
      - "27017:27017"
    volumes:
      - ./data:/data/db